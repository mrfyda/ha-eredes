# CI runs on Python 3.14. Home Assistant >=2026.7 requires Python >=3.14.2.
#
# The entire Home Assistant test stack — homeassistant, pytest, pytest-asyncio,
# pytest-cov, coverage, etc. — is exact-pinned (==) by
# pytest-homeassistant-custom-component to match a specific HA release. Do NOT
# list those packages here: pinning them independently produces a
# ResolutionImpossible conflict. Bump this single package to move the whole
# stack to a newer HA version. (See the Dependabot ignore list in dependabot.yml.)
pytest-homeassistant-custom-component>=0.13.356

# Linting and type checking — not part of the HA test snapshot, so ours to bump.
ruff>=0.16.3
mypy>=2.3.1
